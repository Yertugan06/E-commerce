apiVersion: 1
providers:
  - name: "SLI Dashboards"
    orgId: 1
    folder: "SLIs"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
